import React from 'react';

/**
 * Navigation Component - Minimal component since main nav is in Header
 * This component is kept for backwards compatibility
 */
export default function Navigation() {
  return null; // Navigation is handled in Header component
}
